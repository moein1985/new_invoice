import { describe, it, expect } from '@jest/globals';

describe.skip('documentRouter.list (integration)', () => {
  it('needs a dedicated integration Jest profile for tRPC + superjson runtime', () => {
    expect(true).toBe(true);
  });
});
