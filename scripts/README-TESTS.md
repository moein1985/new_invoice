# 🧪 راهنمای تست‌های جامع سیستم

این فایل شامل توضیحات و راهنمای استفاده از تست‌های جامع سیستم مدیریت اسناد است.

## 📋 فهرست تست‌ها

### 1. **comprehensive-test.ts** - تست اساسی
تست‌های پایه برای بررسی عملکرد اصلی سیستم

```bash
npx tsx scripts/comprehensive-test.ts
```

**تست‌ها:**
- ✅ ایجاد TEMP_PROFORMA با وضعیت PENDING
- ✅ ایجاد Approval با وضعیت PENDING  
- ✅ تایید TEMP_PROFORMA (تغییر به APPROVED)
- ✅ تبدیل TEMP_PROFORMA به PROFORMA با وضعیت APPROVED
- ✅ تبدیل PROFORMA به INVOICE با وضعیت APPROVED
- ✅ بررسی convertedFromId صحیح است
- ✅ ویرایش TEMP_PROFORMA: حذف PROFORMA و INVOICE
- ✅ ویرایش TEMP_PROFORMA: بازگشت به PENDING
- ✅ فقط TEMP_PROFORMA در کارتابل تاییدیه‌ها ظاهر می‌شود
- ✅ ایجاد PROFORMA مستقیم با وضعیت APPROVED

---

### 2. **advanced-workflow-test.ts** - تست پیشرفته (توصیه می‌شود)
تست‌های کامل و جامع برای بررسی تمام سناریوهای ممکن

```bash
npx tsx scripts/advanced-workflow-test.ts
```

## 📊 سناریوهای تست شده

### سناریو 1️⃣: ایجاد → تایید → تبدیل کامل
```
TEMP_PROFORMA (PENDING) 
    → تایید 
    → TEMP_PROFORMA (APPROVED)
    → تبدیل
    → PROFORMA (APPROVED)
    → تبدیل
    → INVOICE (APPROVED)
```

**تست‌ها:**
1. ایجاد TEMP_PROFORMA با PENDING
2. Approval رکورد ایجاد شده است
3. TEMP_PROFORMA در لیست pendingApprovals ظاهر می‌شود
4. تایید TEMP_PROFORMA
5. TEMP_PROFORMA دیگر در pendingApprovals نیست
6. تبدیل TEMP_PROFORMA به PROFORMA با APPROVED
7. PROFORMA مستقیماً APPROVED است (نه PENDING)
8. PROFORMA approval ندارد
9. تبدیل PROFORMA به INVOICE با APPROVED
10. INVOICE مستقیماً APPROVED است

---

### سناریو 2️⃣: ویرایش سند تایید شده
```
TEMP_PROFORMA (APPROVED) + PROFORMA + INVOICE
    → ویرایش TEMP
    → حذف PROFORMA و INVOICE
    → TEMP_PROFORMA بازگشت به PENDING
```

**تست‌ها:**
1. حذف PROFORMA
2. حذف INVOICE
3. بازگشت TEMP_PROFORMA به PENDING
4. TEMP_PROFORMA دوباره در کارتابل ظاهر می‌شود

---

### سناریو 3️⃣: رد کردن سند
```
TEMP_PROFORMA (PENDING)
    → رد
    → TEMP_PROFORMA (REJECTED)
```

**تست‌ها:**
1. ایجاد TEMP_PROFORMA جدید برای رد
2. رد کردن TEMP_PROFORMA
3. TEMP_PROFORMA رد شده در کارتابل نیست

---

### سناریو 4️⃣: ایجاد مستقیم
```
ایجاد مستقیم PROFORMA → وضعیت APPROVED
ایجاد مستقیم INVOICE → وضعیت APPROVED
```

**تست‌ها:**
1. ایجاد مستقیم PROFORMA با APPROVED
2. ایجاد مستقیم INVOICE با APPROVED

---

### سناریو 5️⃣: بررسی آمار داشبورد
```
بررسی صحت شمارش اسناد در داشبورد
```

**تست‌ها:**
1. شمارش صحیح اسناد PENDING
2. شمارش صحیح اسناد APPROVED
3. شمارش صحیح اسناد REJECTED
4. فقط TEMP_PROFORMA با PENDING در کارتابل

---

### سناریو 6️⃣: بررسی ارتباطات (convertedFromId)
```
بررسی صحت ارتباط بین اسناد تبدیل شده
```

**تست‌ها:**
1. ایجاد زنجیره کامل TEMP → PROFORMA → INVOICE
2. PROFORMA.convertedFromId = TEMP_PROFORMA.id
3. INVOICE.convertedFromId = PROFORMA.id
4. یافتن تمام اسناد تبدیل شده از TEMP
5. یافتن تمام اسناد تبدیل شده از PROFORMA

---

## 🎯 نتیجه نهایی

### ✅ موفقیت 100%
همه تست‌ها با موفقیت پاس می‌شوند:
- **28 تست موفق**
- **0 تست ناموفق**
- **درصد موفقیت: 100%**

---

## 📝 قوانین کسب‌و‌کار تست شده

### 1. وضعیت‌های تایید (ApprovalStatus)
- ✅ **TEMP_PROFORMA**: فقط این نوع نیاز به تایید دارد → `PENDING`
- ✅ **PROFORMA**: همیشه `APPROVED` است (تایید خودکار)
- ✅ **INVOICE**: همیشه `APPROVED` است (تایید خودکار)

### 2. Approval Records
- ✅ فقط برای `TEMP_PROFORMA` Approval ایجاد می‌شود
- ✅ `PROFORMA` و `INVOICE` هیچ Approval ندارند

### 3. کارتابل تاییدیه‌ها
- ✅ فقط `TEMP_PROFORMA` با وضعیت `PENDING` در کارتابل ظاهر می‌شود
- ✅ اسناد `APPROVED` و `REJECTED` در کارتابل نیستند
- ✅ `PROFORMA` و `INVOICE` اصلاً در کارتابل ظاهر نمی‌شوند

### 4. ویرایش اسناد
- ✅ ویرایش `TEMP_PROFORMA` تایید شده:
  - اسناد مرتبط (`PROFORMA` و `INVOICE`) حذف می‌شوند
  - وضعیت به `PENDING` برمی‌گردد
  - Approval جدید ایجاد می‌شود

### 5. ارتباطات (convertedFromId)
- ✅ `PROFORMA.convertedFromId` = `TEMP_PROFORMA.id`
- ✅ `INVOICE.convertedFromId` = `PROFORMA.id`
- ✅ زنجیره: `TEMP` → `PROFORMA` → `INVOICE`

### 6. آمار داشبورد
- ✅ تعداد "در انتظار تایید" = تعداد `TEMP_PROFORMA` با `PENDING`
- ✅ تعداد "تایید شده" = تعداد اسناد با `APPROVED`
- ✅ تعداد "رد شده" = تعداد اسناد با `REJECTED`

---

## 🛠️ استفاده از تست‌ها

### قبل از اجرا
مطمئن شوید که دیتابیس در حال اجرا است:
```bash
# بررسی اتصال دیتابیس
npx prisma db pull
```

### اجرای تست‌ها
```bash
# تست اساسی (10 تست)
npx tsx scripts/comprehensive-test.ts

# تست پیشرفته (28 تست) - توصیه می‌شود
npx tsx scripts/advanced-workflow-test.ts
```

### بعد از اجرا
تست‌ها خودکار داده‌های تست را پاک می‌کنند:
```typescript
await prisma.approval.deleteMany();
await prisma.document.deleteMany();
```

---

## 🔍 خروجی تست‌ها

### نمونه خروجی موفق
```
================================================================================
📊 نتیجه نهایی:
================================================================================
✅ موفق: 28
❌ ناموفق: 0
📈 درصد موفقیت: 100.0%
================================================================================

📄 خلاصه وضعیت اسناد:

   📝 TEMP_PROFORMA: 3 سند
      - PENDING: 1
      - APPROVED: 1
      - REJECTED: 1

   📋 PROFORMA: 2 سند
      - همه APPROVED: بله ✅

   🧾 INVOICE: 2 سند
      - همه APPROVED: بله ✅
```

---

## 📚 فایل‌های مرتبط

- `comprehensive-test.ts` - تست اساسی
- `advanced-workflow-test.ts` - تست پیشرفته
- `check-workflow.ts` - بررسی workflow اسناد موجود
- `test-scenario.ts` - تست سناریو خاص

---

## 💡 نکات مهم

1. **قبل از دیپلوی**: حتماً تست‌ها را اجرا کنید
2. **بعد از تغییرات**: هر تغییری در workflow باید تست‌ها را پاس کند
3. **CI/CD**: می‌توانید این تست‌ها را در pipeline خود اضافه کنید

---

## ✨ موفقیت!

اگر همه تست‌ها با موفقیت پاس شدند، سیستم شما آماده استفاده است! 🎉
